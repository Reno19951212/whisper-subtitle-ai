# 廣播字幕製作系統 — 產品需求文件 (PRD)

**版本：** 2.1
**日期：** 2026-04-09
**作者：** Reno Cheung
**狀態：** 進行中

---

## 1. 產品概述

### 1.1 產品定位

一套面向香港廣播電視機構嘅 **本地部署字幕製作系統**。將英文影片內容自動轉錄、翻譯為繁體中文（粵語或書面語）字幕，經人工校對後燒入影片輸出。

### 1.2 核心價值

| 傳統流程 | 本系統 |
|----------|--------|
| 人手聽寫英文逐字稿 | AI 自動語音辨識（Whisper ASR） |
| 人手翻譯英文至中文 | AI 自動翻譯（本地 LLM） |
| 外判翻譯、等待回覆 | 全程本地處理，無需上傳雲端 |
| 多個軟件切換（轉錄→翻譯→字幕→渲染） | 單一系統一站式完成 |
| 術語翻譯不一致 | 術語表自動注入，確保一致性 |

### 1.3 目標用戶

| 角色 | 使用場景 |
|------|----------|
| **字幕編輯** | 日常字幕製作：上傳影片 → 校對翻譯 → 輸出成品 |
| **翻譯主任** | 管理術語表、審批翻譯質素、設定翻譯風格（書面語/粵語） |
| **技術支援** | 部署系統、配置 Profile、管理 AI 模型 |

### 1.4 部署環境

| 環境 | 硬件 | 用途 |
|------|------|------|
| **開發** | MacBook M1 Pro 16GB | 開發測試（Whisper tiny + Mock 翻譯） |
| **生產** | Dell Pro Max GB10（128GB RAM、NVIDIA Blackwell GPU） | 正式製作（大型 ASR + LLM 翻譯） |

### 1.5 關鍵約束

- **全本地部署**：所有 AI 模型本地運行，影片內容不離開內部網絡
- **開源免費**：不依賴任何付費 API 或商業授權
- **瀏覽器操作**：前端為純 HTML/CSS/JS，無需安裝客戶端軟件

---

## 2. 用戶故事

### 2.1 字幕編輯（日常使用）

| ID | 用戶故事 | 驗收準則 |
|----|----------|----------|
| U-01 | 作為字幕編輯，我要上傳英文影片並自動轉錄 | 支援 MP4/MOV/MKV/MXF 等格式；轉錄時顯示進度條及預計時間 |
| U-02 | 作為字幕編輯，我要系統自動將英文字幕翻譯為繁體中文 | 轉錄完成後自動觸發翻譯；翻譯完成後前端即時更新 |
| U-03 | 作為字幕編輯，我要逐句校對和編輯中文翻譯 | 獨立校對頁面；左邊影片右邊字幕表格；點擊即可編輯；鍵盤快捷鍵支援 |
| U-04 | 作為字幕編輯，我要將校對後嘅字幕燒入影片輸出 | 支援 MP4 (H.264) 及 MXF (ProRes 422 HQ)；渲染進度顯示；完成自動下載 |
| U-05 | 作為字幕編輯，我要預覽字幕同步效果 | 播放影片時字幕即時疊加顯示；可調整字幕延遲及大小 |
| U-06 | 作為字幕編輯，我要下載字幕文件 | 每個文件提供 SRT、VTT、TXT 三種格式下載 |
| U-07 | 作為字幕編輯，我要手動觸發翻譯 | 待翻譯狀態顯示「▶ 翻譯」按鈕；翻譯完成後顯示「🔄 重新翻譯」 |

### 2.2 翻譯主任（管理）

| ID | 用戶故事 | 驗收準則 |
|----|----------|----------|
| U-10 | 作為翻譯主任，我要管理術語表確保翻譯一致 | 新增/刪除術語；CSV 匯入/匯出；術語自動注入翻譯 prompt |
| U-11 | 作為翻譯主任，我要切換翻譯風格 | 支援「書面語」及「粵語口語」風格，可即時切換 |
| U-12 | 作為翻譯主任，我要調整翻譯參數 | 可設定 batch size、temperature；按語言獨立配置 |
| U-13 | 作為翻譯主任，我要獨立選擇翻譯引擎及模型 | 下拉選擇引擎（Ollama / Mock / 未來引擎），再選擇該引擎下嘅具體模型及參數 |

### 2.3 技術支援（部署）

| ID | 用戶故事 | 驗收準則 |
|----|----------|----------|
| U-20 | 作為技術支援，我要一鍵部署系統 | `setup.sh` 自動安裝依賴；`start.sh` 一鍵啟動 |
| U-21 | 作為技術支援，我要自由組合 ASR + 翻譯引擎 | 前端可獨立選擇 ASR 引擎/模型 及 翻譯引擎/模型，唔受固定 Profile 限制 |
| U-22 | 作為技術支援，我要為每個引擎配置獨立參數 | 每個 ASR 引擎有自己嘅參數面板（模型大小、語言、device）；每個翻譯引擎有自己嘅參數面板（模型、temperature、style） |
| U-23 | 作為技術支援，我要儲存常用引擎組合為 Profile | 可將當前 ASR + 翻譯 + 字體組合儲存為 Profile，方便快速切換 |
| U-24 | 作為技術支援，我要監控系統狀態 | `/api/health` 端點；模型加載狀態；引擎可用性檢查；連接狀態指示燈 |

---

## 3. 功能需求

### 3.1 核心 Pipeline

| 階段 | 功能 | 輸入 | 輸出 | 狀態 |
|------|------|------|------|------|
| 1. 音頻提取 | FFmpeg 從影片提取音頻 | MP4/MXF 影片 | 16kHz WAV | ✅ 完成 |
| 2. 語音辨識 (ASR) | AI 模型將英文語音轉為文字 | WAV 音頻 | 英文段落 [{start, end, text}] | ✅ 完成 |
| 3. 段落後處理 | 分割過長段落 | ASR 段落 | 優化段落（按語言參數） | ✅ 完成 |
| 4. 翻譯 | AI 模型將英文翻譯為繁體中文 | 英文段落 + 術語表 | 中文翻譯 [{en_text, zh_text}] | ✅ 完成 |
| 5. 校對 | 人工審核、編輯、批核翻譯 | 中文翻譯 | 已批核翻譯 | ✅ 完成 |
| 6. 渲染 | FFmpeg 將字幕燒入影片 | 已批核翻譯 + 字體配置 | MP4/MXF 成品 | ✅ 完成 |

### 3.2 AI 引擎（模塊化選擇）

> **核心設計原則：** ASR 引擎同翻譯引擎完全解耦，可獨立選擇、獨立配置。用戶可自由組合任意 ASR 引擎 + 任意翻譯引擎，唔受固定 Profile 限制。

#### 引擎選擇架構

```
┌─────────────────────────────────────────────────────┐
│                 Pipeline 配置面板                      │
│                                                      │
│  ┌─ ASR 引擎 ──────────────────────────────────┐    │
│  │ 選擇引擎: [ Whisper ▾ ]                       │    │
│  │                                               │    │
│  │ ┌─ Whisper 參數 ───────────────────────┐     │    │
│  │ │ 模型大小: [ small ▾ ]                 │     │    │
│  │ │ 語言:     [ English ▾ ]               │     │    │
│  │ │ Device:   [ auto ▾ ]                  │     │    │
│  │ │ 後端:     [ faster-whisper ▾ ]        │     │    │
│  │ └──────────────────────────────────────┘     │    │
│  └───────────────────────────────────────────────┘    │
│                                                      │
│  ┌─ 翻譯引擎 ─────────────────────────────────┐    │
│  │ 選擇引擎: [ Ollama ▾ ]                       │    │
│  │                                               │    │
│  │ ┌─ Ollama 參數 ───────────────────────┐     │    │
│  │ │ 模型:       [ qwen2.5:3b ▾ ]         │     │    │
│  │ │ 風格:       [ 書面語 ▾ ]              │     │    │
│  │ │ Temperature: [ 0.1 ]                  │     │    │
│  │ │ Batch Size:  [ 10 ]                   │     │    │
│  │ │ 術語表:     [ Broadcast News ▾ ]      │     │    │
│  │ └──────────────────────────────────────┘     │    │
│  └───────────────────────────────────────────────┘    │
│                                                      │
│  [ 💾 儲存為 Profile ] [ 📋 載入 Profile ▾ ]         │
└─────────────────────────────────────────────────────┘
```

#### ASR 引擎

| 引擎 | 可選模型 | 可配參數 | 狀態 |
|------|----------|----------|------|
| **Whisper** | tiny / base / small / medium / large / turbo | model_size, language, device, backend (faster/openai) | ✅ 完成 |
| **Qwen3-ASR** | 待定 | model_size, device | 🔧 Stub |
| **FLG-ASR** | 待定 | model_size, device | 🔧 Stub |

#### 翻譯引擎

| 引擎 | 可選模型 | 可配參數 | 狀態 |
|------|----------|----------|------|
| **Ollama** | qwen2.5:3b / 7b / 72b, qwen3:235b | model, temperature, batch_size, style (formal/cantonese), glossary_id, ollama_url | ✅ 完成 |
| **Mock** | — | — | ✅ 測試用 |
| *(未來擴展)* | | | |
| *vLLM* | *任意 HuggingFace 模型* | *model, temperature, max_tokens* | 📋 規劃中 |
| *OpenAI-compatible* | *GPT-4o / Claude 等* | *api_key, model, temperature* | 📋 規劃中 |

#### 引擎可用性檢測

系統會自動偵測每個引擎嘅可用狀態：

| 引擎 | 檢測方式 | 前端顯示 |
|------|----------|----------|
| Whisper | 檢查 Python 模塊是否已安裝 | ✅ 可用 / ❌ 未安裝 |
| Ollama | 調用 `GET /api/tags` 檢查模型列表 | ✅ 可用 (模型名) / ❌ 未運行 / ⚠️ 模型未下載 |
| Qwen3-ASR | Stub — 永遠返回不可用 | 🔧 待實現 |
| FLG-ASR | Stub — 永遠返回不可用 | 🔧 待實現 |

### 3.3 前端功能

| 頁面 | 功能模塊 | 狀態 |
|------|----------|------|
| **主控台** (index.html) | 文件上傳（拖放/選擇） | ✅ |
| | 轉錄進度條（百分比 + ETA） | ✅ |
| | 翻譯狀態徽章（待翻譯/翻譯中/完成） | ✅ |
| | 手動翻譯/重新翻譯按鈕 | ✅ |
| | **ASR 引擎選擇器**（選引擎 → 顯示該引擎參數面板） | 📋 待實現 |
| | **翻譯引擎選擇器**（選引擎 → 顯示該引擎參數面板） | 📋 待實現 |
| | **引擎可用性狀態**（即時顯示每個引擎是否可用） | 📋 待實現 |
| | Profile 快速切換（載入/儲存當前引擎組合） | ✅ 基礎 / 📋 增強中 |
| | 語言參數配置面板（可收合） | ✅ |
| | 術語表管理面板（可收合） | ✅ |
| | 影片播放 + 字幕疊加預覽 | ✅ |
| | 字幕延遲 / 大小控制 | ✅ |
| | SRT / VTT / TXT 下載 | ✅ |
| **校對編輯器** (proofread.html) | 左右並排：影片 + 字幕表格 | ✅ |
| | 逐句編輯（點擊 / Enter 儲存） | ✅ |
| | 逐句批核 / 批量批核 | ✅ |
| | 鍵盤快捷鍵（↑↓/Enter/E/Esc/Space） | ✅ |
| | 燒入字幕渲染（MP4/MXF 選擇） | ✅ |
| | 渲染進度 + 自動下載 | ✅ |

### 3.4 配置管理

| 功能 | 說明 | 狀態 |
|------|------|------|
| **引擎獨立選擇** | ASR 同翻譯引擎可分別選擇，唔綁定 | 📋 待實現 |
| **引擎參數面板** | 選擇引擎後動態顯示該引擎嘅可配參數 | 📋 待實現 |
| **引擎可用性 API** | 後端偵測引擎/模型是否可用，前端即時顯示 | ✅ 基礎 |
| Profile 系統 | 儲存/載入 ASR + 翻譯 + 字體配置組合 | ✅ |
| 語言參數 | 每語言獨立 ASR/翻譯參數 | ✅ |
| 術語表 | 英中對照 + CSV 匯入/匯出 | ✅ |
| 字體配置 | 字體族、大小、顏色、描邊、位置 | ✅ |

### 3.5 引擎選擇互動流程

```
用戶打開主控台
  │
  ├─ 右側面板顯示：
  │   ├─ 📋 Profile 快速切換（載入預設組合）
  │   │
  │   ├─ 🤖 ASR 引擎
  │   │   ├─ 選擇引擎: [Whisper ▾] [Qwen3-ASR ▾] [FLG-ASR ▾]
  │   │   │   └─ 每個引擎旁顯示 ✅/❌/🔧 可用狀態
  │   │   └─ 選擇後展開該引擎參數:
  │   │       ├─ Whisper: model_size, language, device
  │   │       ├─ Qwen3-ASR: (待實現)
  │   │       └─ FLG-ASR: (待實現)
  │   │
  │   ├─ 🌐 翻譯引擎
  │   │   ├─ 選擇引擎: [Ollama ▾] [Mock ▾]
  │   │   │   └─ 每個引擎旁顯示 ✅/❌ 可用狀態
  │   │   └─ 選擇後展開該引擎參數:
  │   │       ├─ Ollama: model, style, temperature, batch_size, glossary
  │   │       └─ Mock: (無參數)
  │   │
  │   ├─ 🌐 語言配置（可收合）
  │   ├─ 📖 術語表管理（可收合）
  │   └─ [ 💾 儲存為 Profile ] — 將當前選擇儲存為可復用嘅配置
  │
  └─ 上傳影片時使用當前選擇嘅引擎組合
```

### 3.6 後端引擎 API

| 方法 | 路徑 | 說明 | 狀態 |
|------|------|------|------|
| GET | `/api/asr/engines` | 列出所有 ASR 引擎及可用性 | ✅ |
| GET | `/api/asr/engines/<name>/params` | 取得指定引擎嘅可配參數 schema | 📋 待實現 |
| GET | `/api/translation/engines` | 列出所有翻譯引擎及可用性 | ✅ |
| GET | `/api/translation/engines/<name>/params` | 取得指定引擎嘅可配參數 schema | 📋 待實現 |
| GET | `/api/translation/engines/<name>/models` | 列出指定引擎可用嘅模型列表 | 📋 待實現 |

---

## 4. 非功能需求

### 4.1 性能

| 指標 | 目標 |
|------|------|
| 轉錄速度（Whisper small + faster-whisper） | 實時 4-8 倍速 |
| 翻譯延遲（Qwen2.5:3b, batch=10） | < 30 秒 / batch |
| 渲染速度（FFmpeg H.264） | 接近實時 |
| 前端響應 | WebSocket 即時推送，< 100ms UI 更新 |

### 4.2 安全

| 要求 | 說明 |
|------|------|
| 本地部署 | 所有數據及模型在本地運行，不經外部網絡 |
| 無雲端依賴 | 不使用任何付費 API 或雲端服務 |
| 文件隔離 | 上傳文件存儲在 `backend/data/`，可配置清理策略 |

### 4.3 可靠性

| 要求 | 說明 |
|------|------|
| 自動化測試 | 145 個測試覆蓋所有後端模塊 |
| 錯誤處理 | 所有 API 返回結構化錯誤信息 |
| 狀態恢復 | 文件 registry 持久化到磁碟，重啟後恢復 |

### 4.4 兼容性

| 項目 | 支援 |
|------|------|
| 瀏覽器 | Chrome / Firefox / Safari / Edge（現代版本） |
| 操作系統 | macOS / Linux（生產）/ Windows（需 WSL） |
| Python | 3.8+ |
| 影片格式 | MP4, MOV, AVI, MKV, WebM, MXF, MP3, WAV |
| 輸出格式 | MP4 (H.264), MXF (ProRes 422 HQ), SRT, VTT, TXT |

---

## 5. 已知限制及未來方向

### 5.1 已知限制

| 限制 | 說明 |
|------|------|
| ASR 段落碎片化 | Whisper 按 2-5 秒切割，可能喺句子中間斷開 |
| 翻譯重複問題 | 碎片化段落送去 LLM 可能產生重複翻譯（已有實驗性 sentence pipeline，待改進） |
| 單用戶設計 | 目前無用戶認證及多用戶並發支援 |
| Qwen2.5:3b 翻譯質素 | 小模型偶爾產生簡繁混合或英文殘留 |

### 5.2 未來方向

| 優先級 | 方向 | 說明 |
|--------|------|------|
| **高** | **引擎模塊化 UI** | 前端引擎選擇器 + 動態參數面板 + 可用性即時偵測 |
| **高** | **引擎參數 API** | 後端提供每個引擎嘅 param schema + 可用模型列表 API |
| **高** | 改善翻譯質素 | 句子合併 pipeline 改進、更大模型支援、prompt 優化 |
| **高** | 生產 ASR 引擎 | 實現 Qwen3-ASR / FLG-ASR |
| **中** | **新翻譯引擎** | vLLM（HuggingFace 模型直接加載）、OpenAI-compatible API |
| **中** | 多語言支援 | 日文、韓文等目標語言 |
| **中** | 批量處理 | 多文件排隊、批量轉錄+翻譯 |
| **低** | 用戶管理 | 登入、權限、操作日誌 |
| **低** | 字幕樣式模板 | 可保存/載入字幕樣式預設 |

---

## 6. 驗證準則與開發原則

### 6.1 驗證準則（Gate Criteria）

所有功能完成後必須通過以下 gate 先可以標記為完成。呢啲準則適用於任何開發方式（手動、subagent、ralph-loop）。

#### Gate 1：代碼質素
| 檢查項 | 準則 |
|--------|------|
| 測試覆蓋 | 新增功能有對應 test；整體通過率 100% |
| 測試通過 | `pytest tests/ -v` 全部 PASS，0 failures |
| 無 hardcode | 無 hardcoded secrets、URLs、magic numbers |
| 錯誤處理 | 所有 API 端點有 try/catch + JSON error response |
| Python 3.9 兼容 | 唔用 `list[int]` syntax，用 `List[int]` |

#### Gate 2：功能正確性
| 檢查項 | 準則 |
|--------|------|
| API 驗證 | 每個新/改 API 用 curl 測試，確認 status code + response format |
| 前後端一致 | Frontend 調用嘅 API path + request/response format 同 backend 一致 |
| 狀態管理 | WebSocket events 正確觸發；前端 UI 即時更新 |
| Edge cases | 空值、404、無效參數全部有處理 |

#### Gate 3：整合驗證
| 檢查項 | 準則 |
|--------|------|
| 端到端流程 | 相關嘅 pipeline 路徑完整走通 |
| 引擎切換 | 切換 ASR/翻譯引擎後功能正常（如適用） |
| 回歸測試 | 已有功能無 regression |

#### Gate 4：文檔完整性
| 檢查項 | 準則 |
|--------|------|
| CLAUDE.md | 新模塊、新 API、版本記錄已更新 |
| README.md | 用戶可見功能已記錄（繁體中文） |
| PRD / SPEC | 功能狀態標記已更新 |

### 6.2 智能閉環（Ralph-Loop）— 可選

當需要自動化迭代開發時，可使用 `/ralph-loop` 執行閉環。適用場景：多步驟功能、需要反覆調試嘅整合工作。

```
實現 → 測試 → 驗證（Gate 1-4）
  ▲                    │
  └── 不通過 → 修復 ──┘
       通過 → ✅ commit
```

**執行方式：**
```bash
/ralph-loop "實現 [功能名稱]，按照 PRD x.x 節規格。
完成條件：
1. pytest 全部通過
2. curl 測試新 API 返回正確格式
3. 前端功能可操作
4. CLAUDE.md + README.md 已更新
" --completion-promise "DONE" --max-iterations 15
```

**拆分原則：** 如果預計超過 15 次迭代，拆分成更小嘅獨立功能單元。

### 6.3 功能開發拆分原則

無論用咩開發方式，大型功能都應該拆分為獨立可驗證嘅單元：

| 粒度 | 例子 | 驗證方式 |
|------|------|----------|
| **小** | 新增一個 API endpoint + test | 手動或 subagent |
| **中** | 引擎參數 schema API + 前端動態面板 | Subagent 或 ralph-loop |
| **大** | 完整引擎模塊化（後端 + 前端 + 測試） | 拆成 3-4 個中型單元 |

---

## 7. 成功指標

| 指標 | 目標 |
|------|------|
| 轉錄準確度（英文 WER） | Whisper small: < 10% |
| 翻譯可用率（無需大幅修改嘅段落比例） | > 70% |
| 端到端處理時間（5 分鐘影片） | < 10 分鐘（轉錄+翻譯+校對+渲染） |
| 系統穩定性 | 連續運行 8 小時無 crash |
| 測試覆蓋率 | 後端 > 80% |
